# Terminology Resources

- `extraction/`: scripts/notebooks to build terminology lists from corpora or external resources.
- `alignment/`: link terms to occurrences in the parallel training data.
- `resources/`: final term lists used during training and inference.
