# Experiment Configurations

Store YAML/JSON configuration files describing each experiment:
- Data paths (train/dev/test, terminology resources)
- Model parameters (baseline vs terminology-aware)
- Training and decoding settings
